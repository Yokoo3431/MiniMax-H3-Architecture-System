# MiniMax H3 Core System Assets

> [!NOTE]
> **Git Managed Core**: The `core/` directory contains the version-controlled system assets for the MiniMax H3 Architecture Platform.
> Updating the system via `launcher/Update_H3.bat` will update `core/` while preserving `userdata/`.
