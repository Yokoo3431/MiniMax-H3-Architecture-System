{
  "gate_version": "1.0.0",
  "system_name": "MiniMax H3 Architect Production Ready Gate Manifest for V0.8.0",
  "v0_8_0_readiness_target": "Architect Production Ready",
  "rules": {
    "workflow_execution_success": {
      "required": true,
      "description": "ComfyUI workflow API execution must complete without unhandled runtime exceptions"
    },
    "real_output_file_generated": {
      "required": true,
      "min_file_size_bytes": 1048576,
      "description": "Real MP4 video file must exist and have non-zero file size"
    },
    "production_resolution": {
      "required": true,
      "min_width": 1280,
      "min_height": 720,
      "description": "Video resolution must meet minimum production target (1280x720)"
    },
    "no_critical_deformation": {
      "required": true,
      "min_geometry_score": 85.0,
      "description": "Structural facade grid, rooflines, and volume massing must remain undeformed"
    },
    "case_package_completeness": {
      "required": true,
      "required_elements": [
        "input_architectural_images",
        "workflow_json",
        "generated_h3_official_prompt",
        "output_mp4_metadata",
        "representative_frames"
      ]
    }
  }
}
